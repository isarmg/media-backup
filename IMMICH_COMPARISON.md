# Immich 参考与独立实现说明

## 参考基线

- 参考项目：Immich `3.1.0`，本地参考提交 `c98c20e9639257187e1e3b2efa9f1a7f3b465a9d`。
- 参考范围：用户可观察的备份/恢复流程、相册选择、时间线、组织能力、重复项和运维接口。
- 实现方式：本项目自行设计 Rust/Axum/SQLx 协议、当前版 SQLite Schema 以及 Swift/Kotlin 客户端；没有复制 Immich 源码、资源、数据库 Schema 或生成物。

## 架构选择

Immich 是完整的照片管理平台，服务端读取原始媒体以完成 EXIF、转码、机器学习、搜索与分享。本项目仍保持“小而专”的备份定位，但根据产品要求改为：

- HTTPS 保护传输过程；
- 服务端以 `plain-v1` 保存原始未加密字节；
- 设备侧生成缩略图，服务器只负责鉴权存取；
- 单一当前版 SQLite 保存可查询的相册、标签、收藏、归档和同步状态；
- 不引入 Valkey、机器学习服务、视频转码或完整 Web 图库。

这一选择显著简化新设备恢复和图库组织，也意味着服务器/数据卷管理员能够读取媒体。它不是零知识或端到端加密方案，部署安全依赖 TLS、主机隔离、磁盘加密、访问控制和可靠备份。

## 已独立实现的九项能力

| 能力 | 本项目实现 | 与 Immich 的范围差异 |
|---|---|---|
| 新设备恢复 | 登录、完整分页时间线、单项/批量写回系统照片库 | 不提供完整 Web 图库 |
| 相册选择/排除 | PhotoKit、MediaStore 原生相册选择 | UI 更轻量 |
| 单向相册同步 | 服务端相册及成员关系，完整扫描精确替换 | 无共享相册/协作 |
| 缩略图/时间线 | 设备生成缩略图、鉴权展示、原图下载 | 无服务端转码管线 |
| 分页/增量同步 | 不透明时间线游标、单调 change sequence | 未实现离线图库数据库 |
| 收藏/归档/标签 | SQLite、筛选 API、两端基础操作 | 标签管理 UI 较简化 |
| 回收站/撤销 | 软删除、恢复、受限永久删除 | 暂无自动清空保留策略 |
| 重复项管理 | 原始内容 BLAKE3 分组，结合回收站处理 | 无相似图片识别 |
| API/审计/监控 | 可撤销 API Key、分页审计、Prometheus 指标 | 无 OpenTelemetry 全链路追踪 |

## 有意不实现的部分

当前不引入人脸识别、语义搜索、地图、分享链接、视频转码、RAW 派生、合作伙伴共享和多级作业集群。这些能力会把项目从备份工具推向完整媒体平台，增加大量依赖、升级成本和安全面；需要时应优先独立评估，而不是继续以“参考功能”名义无限扩张。产品进程只初始化并严格验证 Photo Backup 0.2.0 当前 Schema，不包含跨版本 migration、backup 或 restore；这些离线职责属于独立升级工具。

## 许可证

Immich 使用 AGPL-3.0，本项目使用 Apache-2.0。研究公开行为和架构思想不等于复制实现；任何未来贡献都必须保持来源可审计，禁止把 `reference-immich/` 中的代码、样式、资源、测试或 Schema 变更脚本直接带入第一方目录。
