# 本项目与 Immich 对比分析

## 1. 对比基线

- 本项目：当前工作目录中的源码（目录本身不是 Git 仓库，因此没有可引用的提交号）。
- Immich：`https://github.com/isarmg/immich.git`，以 `--depth 1` 拉取到 `reference-immich/`。
- Immich 分析提交：`c98c20e9639257187e1e3b2efa9f1a7f3b465a9d`，提交时间 2026-08-23，版本号 `3.1.0`。
- 本次是当前代码快照的静态分析，不是两个 Git 历史之间的逐提交 diff。

## 2. 核心结论

两者解决的问题有交集，但产品目标不同：

- 本项目是一个“小而专”的私有备份系统，核心价值是客户端端到端加密、断点续传、幂等上传和移动端后台备份。
- Immich 是完整的照片与视频管理平台，核心价值是图库浏览、搜索、分享、相册、地图、人脸识别、转码和多用户协作。
- 本项目的服务器拿不到照片明文；Immich 的服务器必须读取媒体明文，才能提取 EXIF、生成缩略图、转码和运行机器学习。这是最根本的架构分叉，不能通过简单移植几个模块消除。
- 如果目标是“可信、低运维、只备份”，继续强化本项目更合适；如果目标是“Google Photos 式体验”，直接部署或二次开发 Immich 的成本远低于在本项目上重造完整图库。
- 最有价值的折中方向是“双层架构”：本项目保留为加密归档层，Immich 作为用户主动导入的可浏览媒体层。不要让两套系统直接共用或修改彼此的存储目录。

## 3. 规模与成熟度

以下数字按本次检出的源码统计，仅用于量级比较：

| 指标 | 本项目 | Immich |
|---|---:|---:|
| 主要源码/配置文件 | 38 | 2,353 |
| 主要源码行数 | 约 4,425 | 约 267,728 |
| 后端 API 组织 | 1 个 Axum 路由模块 | 47 个 NestJS Controller |
| 数据库迁移 | 2 | 95 |
| 自动化测试文件/测试标记 | 未发现 Rust `#[test]` | 约 378 个测试文件 |
| 仓库跟踪文件总数 | 当前目录无 Git 元数据 | 3,431 |

Immich 的代码规模约为本项目的 60 倍。这个差距主要来自完整 Web 应用、跨平台移动端、媒体处理、机器学习、共享协作、国际化、可观测性和大量测试，并不意味着本项目的备份内核本身需要扩张到相同规模。

## 4. 架构对比

| 维度 | 本项目 | Immich |
|---|---|---|
| 服务端 | Rust、Axum、SQLx | TypeScript、NestJS、Kysely、BullMQ |
| Web | 内嵌的轻量管理页 | SvelteKit 完整图库与管理界面 |
| Android | Kotlin + Compose，Rust 通过 JNI 共享核心 | Flutter，另有平台原生集成 |
| iOS | SwiftUI，Rust FFI 共享核心 | Flutter，另有平台原生集成 |
| 客户端状态 | SQLite 持久化上传状态机 | Drift/SQLite 与更完整的图库、同步状态 |
| 数据库 | PostgreSQL 17 | 定制 PostgreSQL 14 镜像，包含向量搜索扩展 |
| 队列/缓存 | 无独立队列服务 | Valkey + BullMQ |
| 机器学习 | 无 | 独立 Python/FastAPI/ONNX 服务 |
| 媒体处理 | 客户端加密、服务端只存 blob | EXIF、缩略图、视频转码、RAW、Live Photo 等 |
| 对象存储 | 本地文件系统抽象 | `/data` 媒体库与派生文件体系 |
| 默认端口 | 8080 | 2283 |

本项目的共享 Rust 核心是一个明显优势：加密、去重和上传状态机可在两端复用，安全边界较集中。Immich 则选择产品能力优先的多语言架构，各服务针对 UI、媒体管线和 ML 分工。

## 5. 安全与隐私模型

### 本项目

- 文件先在设备侧以随机资产密钥进行 XChaCha20-Poly1305 分块加密。
- 资产密钥再由设备主密钥封装；元数据也可加密。
- 明文文件的 BLAKE3 摘要不会直接上传，而是通过账户去重密钥做 HMAC，形成账户内去重令牌。
- 服务端只验证密文分块大小和 BLAKE3，不能解密内容。
- Bearer Token 在数据库中只保存 SHA-256 摘要。
- 默认“手机删除不删除服务端副本”，更符合备份语义。

当前最严重的安全/可用性缺口是密钥生命周期：每台新设备默认生成自己的主密钥与去重密钥，尚无恢复密钥导入、二维码配对、密钥轮换或灾难恢复流程。因此系统已经能安全上传，但还不能可靠保证“换手机后一定能恢复”。

另外，服务端虽有加密内容下载 API，当前移动端 UI 未形成完整的浏览、选择、下载、解密、写回系统照片库的恢复闭环。

### Immich

本次源码中未发现客户端端到端媒体加密机制。服务端直接管理原始媒体路径，并读取原文件进行元数据提取、缩略图生成、视频处理和机器学习。这意味着：

- HTTPS 可以保护传输，但服务器、挂载卷管理员和服务器端处理进程可以接触媒体明文。
- 服务器侧搜索、人物识别、地图、转码和 Web 即时浏览因此能够实现。
- 若宿主机或存储卷泄露，需要依靠磁盘/卷加密、主机权限隔离与备份加密降低风险，而不是依赖零知识设计。

这不是简单的“谁更安全”，而是隐私与功能之间的产品选择：端到端加密天然限制服务端理解和加工内容。

## 6. 功能覆盖

| 功能 | 本项目 | Immich |
|---|---|---|
| iOS/Android 自动备份 | 已实现基础流程 | 成熟实现 |
| 原图/视频上传 | 是 | 是 |
| 分块、断点续传、幂等提交 | 明确设计并实现 | 有成熟上传与同步体系 |
| 账户内去重 | 加密 HMAC 去重 | 文件校验和去重 |
| 端到端加密 | 是 | 否（未发现相关机制） |
| Web 时间线/图库 | 否 | 是 |
| 相册、共享相册、公开链接 | 否 | 是 |
| 多用户与合作伙伴共享 | 仅基础账户/设备管理 | 是 |
| EXIF、地图、RAW | 元数据以密文存储，暂无展示 | 是 |
| 人脸识别、语义搜索 | 否 | 是 |
| 视频转码/缩略图 | 否 | 是 |
| OAuth、API Key | 否 | 是 |
| 完整恢复体验 | 未完成 | 可下载/回看，但不是 E2EE 恢复模型 |
| 存储配额 | 基础账户配额 | 完整用户与存储管理 |
| 离线图库 | 否 | 移动端支持 |

## 7. 部署与运维

本项目只需要两个主要容器：应用服务和 PostgreSQL。优点是资源占用和故障面较小，升级路径也容易理解。缺点是目前缺少成熟产品通常需要的监控指标、任务队列、备份校验工具、存储修复命令、对象存储后端和完善升级文档。

Immich 默认包含服务端、机器学习、Valkey 和带向量扩展的 PostgreSQL，运行时还需要媒体处理工具和模型缓存。它的部署和升级更重，但已经覆盖：

- 后台作业与失败重试；
- 缩略图、转码和 ML 管线；
- Prometheus/OpenTelemetry 相关依赖；
- 硬件加速选项；
- 更成熟的迁移、管理和故障排查体系。

两者都不能替代真正的备份策略。数据库与媒体/blob 必须一致备份，并至少遵循 3-2-1 原则。Immich README 也明确提醒不要把 Immich 当成唯一副本。

## 8. 代码质量与风险

本次执行 `cargo test --workspace` 成功，5 个 Rust crate 和文档测试均能编译运行；但所有测试目标合计执行 0 个测试，并出现 `crates/agent-core/src/lib.rs` 中未使用 `PathBuf` 导入的警告。

### 本项目的优势

- 领域边界清晰，协议、加密、状态机、FFI、服务端分 crate 组织。
- 上传协议显式建模缺失分块、密文校验、原子完成和账户配额。
- Rust 核心减少两套移动端对关键算法的重复实现。
- 服务端无法读取用户内容，攻击面和隐私责任显著低于完整媒体平台。

### 本项目当前风险

1. **没有自动化测试**：加密格式、空文件、分块边界、崩溃恢复、重复提交、配额并发、跨版本兼容都缺少回归保障。
2. **恢复链路未闭环**：有下载接口，不等于用户能在新设备上恢复。
3. **密钥管理未产品化**：无跨设备配对、恢复码、轮换、撤销后的数据访问策略。
4. **协议版本化不足**：AAD 中有 `v1`，但 API/清单的长期兼容和迁移策略尚不完整。
5. **存储后端单一**：本地目录适合小规模，但缺 S3/MinIO、校验巡检、垃圾回收和孤儿分块清理。
6. **iOS/Android 能力不对称**：Android 设置和调度界面明显更完整，iOS 当前界面与恢复体验更薄。
7. **许可证落地不完整**：Cargo 声明 Apache-2.0，但根目录未见对应 `LICENSE` 文件。
8. **工程不是 Git 仓库**：无法审计变更历史、建立可靠发布标签或方便地回滚。

### Immich 的优势与代价

- 功能、测试、社区经验和平台兼容性远成熟于本项目。
- 复杂度、资源消耗、升级要求和攻击面也明显更高。
- 其丰富功能依赖服务器接触明文，无法原样移植到零知识服务端。
- 上游快速演进，生产环境应跟随发布版 compose，而不是直接部署 `main` 分支配置。

## 9. 许可证影响

- 本项目在 Cargo 元数据中声明 Apache-2.0。
- Immich 使用 AGPL-3.0。

可以研究其架构和重新实现思想，但若直接复制或修改 Immich 代码并将其组成网络服务，通常需要遵守 AGPL 的源码提供义务。不能把引入的 AGPL 派生代码继续当作纯 Apache-2.0 项目发布。具体边界涉及法律判断，正式商用前应进行许可证审查。

## 10. 建议路线

### 如果坚持“端到端加密备份”定位

优先级建议如下：

1. 为加密格式和上传状态机建立测试矩阵，锁定 v1 文件格式。
2. 完成恢复密钥、二维码配对、新设备导入和撤销流程。
3. 做出端到端恢复 UI，并进行真实设备的大文件、断网、杀进程和换机测试。
4. 增加对象存储接口、后台完整性巡检、孤儿上传回收和导出工具。
5. 增加指标、结构化审计日志、速率限制和凭据轮换。
6. 再考虑只依赖客户端解密的本地图库、缩略图或搜索；不要让服务器静默获得主密钥。

可借鉴 Immich 的设计包括：移动端相册选择和备份 UX、作业可观测性、OpenAPI SDK、数据库迁移纪律、管理员体验、发布流程和端到端测试。不要直接照搬依赖明文媒体的 ML、缩略图与服务端检索管线。

### 如果目标是尽快获得完整照片管理产品

直接采用 Immich，并把工程投入放在部署加固、磁盘加密、反向代理、外部备份、升级演练和必要的品牌/流程定制上。不要在当前 4 千行级别项目上逐项复刻 Immich 的 26 万行功能面。

### 如果两种目标都要

推荐将两者隔离：

1. 本项目保存长期、零知识的加密原始归档。
2. 用户选择性地在可信设备上解密并导入 Immich。
3. Immich 负责浏览、搜索、分享和媒体处理。
4. 两者使用独立数据库和独立存储根目录，通过显式导入/导出交接，不直接共享内部文件布局。

这种设计会增加一份存储开销，但安全边界清楚，升级时也不会让一个系统破坏另一个系统的数据一致性。
